# AMD ROCm GPU overlay. Enable by setting COMPOSE_FILE in .env:
#   COMPOSE_FILE=docker/compose.yml:docker/gpu.amd.yml
#   RENDER_GID=<numeric output of: getent group render | cut -d: -f3>
#
# Requires ROCm drivers on the host (kfd + DRI devices). The host user
# running Docker must be in the `video` and `render` groups.
#
# This overlay passes the host GPU through to the container and requires
# Dockerfile.gpu-amd (build: { dockerfile: Dockerfile.gpu-amd }) which
# pre-compiles llama-server with ROCm/HIP support at image build time.
services:
  atenea:
    devices:
      - /dev/kfd
      - /dev/dri
    group_add:
      - video
      - ${RENDER_GID:-render}
