"""Action and intent registry.

The action surface used by the agent, scheduled tasks, and slash
commands lives here. ``builtin.py`` holds the action handlers;
``intents.py`` and ``document.py`` are domain-specific helpers.
"""
