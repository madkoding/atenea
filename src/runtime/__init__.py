"""Runtime bootstrap: app initialization, helpers, runtime config.

The app entry point (``app.py``) imports ``initialize_managers`` from
``src.runtime.app_initializer``. Other modules in this package are
small, dependency-free helpers used during app construction and
shutdown.
"""
