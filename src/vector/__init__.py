"""Vector / embedding / RAG runtime.

This package groups ChromaDB plumbing, embedding lanes, and the RAG
manager. Most consumers should import the specific submodule; the
``src.vector`` namespace exists for future re-exports only.
"""
