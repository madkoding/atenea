"""Scheduling, background jobs, and lifecycle helpers.

This package groups the in-process task scheduler, the event bus, the
background-jobs runtime (and its monitor), and cookbook serve lifecycle.
Submodules keep their historical names — the new fully-qualified import
is ``src.scheduling.X`` instead of ``src.X``.
"""
