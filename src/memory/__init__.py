"""Persistent memory and the memory provider abstraction.

The vector index for memory lives in ``src.vector`` (ChromaDB-backed).
This package holds the JSON-backed store, the provider abstraction
that the rest of the app talks to, and any non-vector helpers.
"""
