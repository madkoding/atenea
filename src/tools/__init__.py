"""Tool runtime: schemas, parsing, policy, security, execution, implementations, index.

This package groups the tool runtime. Most consumers should keep importing
``from src.tools.X import ...`` (where X is the specific submodule). The
facade re-exports that previously lived in ``src.agent.tools_facade`` are still
available there for backward compatibility.
"""
