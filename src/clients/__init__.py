"""External-service clients.

``api_key_manager`` — encrypted storage of provider keys.
``copilot`` — GitHub Copilot device-flow login.
``mcp_manager`` — MCP server registry / connection pool.
``mcp_oauth`` — OAuth handshake for MCP servers.
"""
